import { CreateStartupFlow } from '@/components/startup/create/CreateStartupFlow'

/** The "Create Startup" route (`/startups/new`): the six-step flow. */
export default function RegisterStartupPage() {
  return <CreateStartupFlow />
}
